# Doran — Tauri Example

Reference shell for embedding a Doran-powered React UI in a Tauri desktop app. Use the
[`examples/react`](../react) front-end as the Tauri `frontendDist`, or scaffold a new app
with `pnpm create tauri-app` and reuse the same components.

> Scaffolding placeholder.
