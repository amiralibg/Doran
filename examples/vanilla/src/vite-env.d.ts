/// <reference types="vite/client" />
// Enables typing for `import src from './Demo?raw'` — the raw text of a module,
// which the demo cards display verbatim so the shown code can never drift from
// the code that actually runs.
